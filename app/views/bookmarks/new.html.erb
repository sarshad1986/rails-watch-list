<h1>Add a movie</h1>

<div class="container">
  <%= simple_form_for [@list, @bookmark] do |f| %>
    <%= f.association :movie %>
    <%= f.input :comment %>
    <%= f.button :submit, "Add Movie", class: "btn btn-primary" %>
  <% end %>
</div>
